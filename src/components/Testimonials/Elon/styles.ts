import styled from "styled-components";

import { TestimonialContainer } from "../../../styles/Global/styles";

export const ElonContainer = styled(TestimonialContainer)`
  &::after {
    top: 7.8125rem;
  }
`