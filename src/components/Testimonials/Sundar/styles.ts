import styled from "styled-components";

import { TestimonialContainer } from "../../../styles/Global/styles";

export const SundarContainer = styled(TestimonialContainer)`
  &::after {
    top: 1.375rem;
  }
`