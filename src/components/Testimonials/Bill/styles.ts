import styled from "styled-components";

import { TestimonialContainer } from "../../../styles/Global/styles";

export const BillContainer = styled(TestimonialContainer)`
  &::after {
    top: 20.6875rem;
  }
`