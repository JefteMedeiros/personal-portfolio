import styled from "styled-components";

import { TestimonialContainer } from "../../../styles/Global/styles";

export const TimContainer = styled(TestimonialContainer)`
  position: relative;

  &::after {
    top: 14.25rem;
  }
`
